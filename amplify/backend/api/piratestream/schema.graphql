type WatchHistory @model @auth(rules: [{allow: owner}]) {
  id: ID!
  userId: String!
  imdbID: String!
  title: String!
  type: String!
  season: String
  episode: String
  episodeTitle: String
  progress: Float
  timestamp: AWSTimestamp!
  poster: String
  tmdbId: String
}